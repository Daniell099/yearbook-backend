info:
  name: " Buscar aluno inexistente"
  type: http
  seq: 2

http:
  method: GET
  url: https://legendary-system-pjxp5xjq9pj9f6j5q-3000.app.github.dev/alunos/999
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("deve retornar status 404 para aluno inexistente", function() {
          expect(res.getStatus()).to.equal(404);
        });

        test("deve retornar mensagem de erro", function() {
          expect(res.getBody().erro).to.equal('Aluno não encontrado');
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
